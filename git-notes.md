---

## 4. Как теперь это загрузить в `devops-notes`

1. В VSCode (подключённом к серверу) открой папку `devops-notes` (или склонируй её туда, если её ещё нет на сервере).
2. Создай файл `docker-notes.md` и вставь туда эту структуру (можешь редактировать, добавлять свои примеры).
3. Сделай коммит и пушь:
   ```bash
   git add docker-notes.md
   git commit -m "Добавил шпаргалку по Docker"
   git push
