markdown
# Linux Command Cheat Sheet (На основе боевого опыта)

## 📁 Навигация и работа с файлами

| Команда | Что делает | Основные аргументы | Пример | Аналог в PowerShell |
|---------|-----------|---------------------|--------|---------------------|
| `pwd` | Показать текущую папку | | `pwd` | `Get-Location` |
| `ls` | Показать содержимое папки | `-l` (подробно), `-a` (включая скрытые), `-h` (читаемые размеры) | `ls -la` | `Get-ChildItem` (или `dir`, `ls`) |
| `cd` | Сменить текущую папку | без аргументов — в домашнюю папку | `cd /home/user` | `Set-Location` (или `cd`) |
| `cd ..` | Перейти на уровень выше | | `cd ..` | `cd ..` |
| `cd ~` | Перейти в домашнюю папку | | `cd ~` | `cd ~` |
| `touch файл` | Создать пустой файл | | `touch file.txt` | `New-Item -ItemType File` |
| `mkdir папка` | Создать папку | `-p` (создать промежуточные) | `mkdir -p папка/подпапка` | `New-Item -ItemType Directory` |
| `rm файл` | Удалить файл | | `rm file.txt` | `Remove-Item` |
| `rm -r папка` | Удалить папку с содержимым | `-f` (без подтверждения) | `rm -rf папка` | `Remove-Item -Recurse` |
| `cp файл1 файл2` | Скопировать файл | `-r` (рекурсивно для папок), `-i` (спрашивать), `-v` (подробно) | `cp -r папка1 папка2` | `Copy-Item` |
| `mv файл1 папка/` | Переместить файл | `-i` (спрашивать), `-v` (подробно) | `mv file1.txt file2.txt` | `Move-Item` |

---

## 🔍 Просмотр и поиск

| Команда | Что делает | Основные аргументы | Пример | Аналог в PowerShell |
|---------|-----------|---------------------|--------|---------------------|
| `cat файл` | Вывести содержимое файла | | `cat file.txt` | `Get-Content` |
| `less файл` | Просмотр файла постранично | `q` — выход | `less bigfile.log` | `more` или `Out-Host -Paging` |
| `head файл` | Первые 10 строк | `-n 20` (сколько строк) | `head -n 20 file.txt` | `Get-Content -Head` |
| `tail файл` | Последние 10 строк | `-n 20`, `-f` (следить за изменениями) | `tail -f log.txt` | `Get-Content -Tail -Wait` |
| `grep слово файл` | Найти слово в файле | `-i` (без учёта регистра), `-r` (рекурсивно), `-n` (номера строк) | `grep -i "error" log.txt` | `Select-String` |
| `grep -r слово папка` | Рекурсивный поиск | | `grep -r "TODO" ./src` | `Select-String -Recurse` |
| `find папка -name "имя"` | Найти файлы по имени | | `find /home -name "*.txt"` | `Get-ChildItem -Recurse -Filter` |

---

## 💾 Информация о системе, дисках и разделах

| Команда | Что делает | Пример |
|---------|-----------|--------|
| `lsblk` | Показывает все диски и разделы деревом | `lsblk` |
| `lsblk -f` | То же + файловая система (ext4/ntfs/swap) и UUID | `lsblk -f` |
| `lsblk -o NAME,SIZE,MODEL` | Имя, размер, модель диска | `lsblk -o NAME,SIZE,MODEL` |
| `sudo fdisk -l` | Детальная информация о разделах (тип, сектора) | `sudo fdisk -l` |
| `sudo blkid` | UUID и тип ФС для всех разделов | `sudo blkid` |
| `df -h` | Свободное место на смонтированных дисках | `df -h` |
| `free -h` | Информация об оперативной памяти | `free -h` |
| `uname -a` | Информация о ядре и системе | `uname -a` |
| `whoami` | Имя текущего пользователя | `whoami` |
| `id` | Информация о пользователе и группах | `id` |
| `systemctl status` | Статус службы | `systemctl status nginx` |
| `journalctl -xe` | Просмотр системных логов | `journalctl -xe` |
| `sudo dmesg | grep -i usb` | Логи по USB-устройствам | `sudo dmesg | grep -i usb` |
| `sudo dmesg -w` | Следить за логами в реальном времени | `sudo dmesg -w` |

---

## 🛠️ Работа с дисками и разделами

| Команда | Что делает | Важно! |
|---------|-----------|--------|
| `sudo fdisk /dev/sdX` | Редактор разделов (MBR). Внутри: `p` (показать), `d` (удалить), `n` (новый), `w` (записать) | **Не забудь `w`**, иначе изменения не сохранятся. |
| `sudo gdisk /dev/sdX` | То же для GPT (UEFI) | Для современных систем. |
| `sudo mkfs.ext4 /dev/sdX1` | Форматирует раздел в ext4 | **Все данные на разделе будут уничтожены.** |
| `sudo mkswap /dev/sdX2` | Делает раздел swap-областью | После создания включи: `swapon`. |
| `sudo swapon /dev/sdX2` | Включает swap-раздел | Проверить: `swapon --show`. |
| `sudo wipefs -a /dev/sdX` | Стирает **ВСЕ** файловые системы и таблицу разделов с диска | **Полная зачистка. Данные не восстановить.** |
| `sudo dd if=/dev/zero of=/dev/sdX bs=512 count=1` | Затирает первый сектор (убивает таблицу разделов) | Мягче, чем `wipefs`. Тоже необратимо. |

---

## 🔗 Монтирование и Chroot

| Команда | Что делает | Пример |
|---------|-----------|--------|
| `sudo mount /dev/sdX1 /mnt` | Примонтировать раздел в папку `/mnt` | `sudo mount /dev/sda2 /mnt` |
| `sudo umount /mnt` | Отмонтировать | `sudo umount /mnt` |
| `sudo mkdir -p /mnt/boot/efi` | Создать папку для EFI | |
| `sudo mount /dev/sdX1 /mnt/boot/efi` | Примонтировать ESP-раздел | `sudo mount /dev/sda1 /mnt/boot/efi` |
| `sudo mount --bind /dev /mnt/dev` | Примонтировать системные папки | |
| `sudo mount --bind /proc /mnt/proc` | ...для работы chroot | |
| `sudo mount --bind /sys /mnt/sys` | ... | |
| `sudo chroot /mnt` | «Зайти» внутрь примонтированной системы | После этого можно чинить загрузчик. |

---

## 🧰 Процессы и права

| Команда | Что делает | Пример |
|---------|-----------|--------|
| `ps aux` | Показать все запущенные процессы | `ps aux | grep firefox` |
| `kill PID` | Убить процесс по номеру | `kill -9 1234` |
| `killall имя` | Убить все процессы с именем | `killall firefox` |
| `chmod 755 файл` | Установить права (rwxr-xr-x) | `chmod +x script.sh` |
| `chown user:group файл` | Сменить владельца | `sudo chown user:user file.txt` |

---

## 🌐 Сеть

| Команда | Что делает | Пример | Аналог в PowerShell |
|---------|-----------|--------|---------------------|
| `ip a` | Показать IP-адреса | `ip a` | `Get-NetIPAddress` |
| `ip link` | Состояние сетевых интерфейсов | `ip link set eth0 up` | `Enable-NetAdapter` |
| `ping ya.ru` | Проверить доступность узла | `ping ya.ru` | `Test-Connection` |
| `netstat -tulpn` | Показать открытые порты | `netstat -tulpn` | `Get-NetTCPConnection` |
| `ss -tulpn` | Более современный аналог netstat | `ss -tulpn` | |
| `curl ifconfig.me` | Узнать внешний IP | `curl ifconfig.me` | |
| `wget ссылка` | Скачать файл из интернета | `wget https://example.com/file.zip` | `Invoke-WebRequest` |

---

## 📦 Управление пакетами (APT)

| Команда | Описание |
|---------|----------|
| `sudo apt update` | Обновить список пакетов |
| `sudo apt upgrade` | Обновить все пакеты |
| `sudo apt install пакет` | Установить пакет |
| `sudo apt remove пакет` | Удалить пакет |
| `sudo apt purge пакет` | Удалить пакет с конфигами |
| `sudo apt --fix-broken install` | Починить сломанные зависимости |
| `apt search слово` | Найти пакет |

### Работа с .deb-файлами
```bash
# Скачать .deb-пакет с переименованием
wget -O имя.deb "ссылка_на_файл"

# Установить скачанный пакет
sudo apt install ./имя.deb
Ошибка прав при установке .deb
Если возникает ошибка "файл недоступен для пользователя _apt", перемести файл в /tmp:

bash
sudo mv /home/user/пакет.deb /tmp/
sudo apt install /tmp/пакет.deb
🔑 Полезные команды и сокращения
Команда	Что делает
man команда	Показать документацию по команде (выход q)
команда --help	Краткая справка
which команда	Показать путь к исполняемому файлу
history	Показать историю введённых команд
Ctrl + R	Поиск по истории команд
sudo !!	Выполнить предыдущую команду с sudo
sudo -i	Войти в root (чтобы не писать sudo перед каждой командой)
📦 Архивация
Команда	Описание
tar -czf архив.tar.gz папка	Создать архив .tar.gz
tar -xzf архив.tar.gz	Распаковать архив
zip -r архив.zip папка	Создать zip-архив
unzip архив.zip	Распаковать zip
🧠 Основные концепции
Концепция	Краткое описание
UEFI vs Legacy	UEFI — современный режим загрузки, требует ESP-раздел (FAT32). Legacy — старый, загрузчик в MBR.
GPT vs MBR	GPT — для UEFI, до 128 разделов, диски >2 ТБ. MBR — для Legacy, до 4 основных разделов.
GRUB	Загрузчик Linux. Конфигурация в /boot/grub/grub.cfg.
chroot	Смена корневой директории. Позволяет зайти в «сломанную» систему и чинить её.
ESP (EFI System Partition)	Раздел 100-500 МБ, FAT32, флаги boot, esp. Содержит загрузчики ОС.
🚫 Типичные ошибки и их решения
Ошибка	Причина	Решение
Device or resource busy	Раздел примонтирован	sudo umount -l /dev/sdX*, затем wipefs
grub-install: cannot find EFI directory	ESP не примонтирован в /boot/efi	sudo mount /dev/sdX1 /mnt/boot/efi
E: Could not get lock /var/lib/apt/lists/lock	Другой процесс apt уже запущен	sudo kill <PID> или перезагрузить компьютер
Permission denied при установке .deb	apt не имеет прав читать файл в домашней папке	Переместить файл в /tmp
No working init found	Ядру передан неверный UUID корневого раздела	Проверить grub.cfg, поправить при загрузке через e

cd папка — перейти в папку (как cd в cmd, но в PowerShell тоже cd работает).

Чтобы запустить скрипт, нужно:

Перейти в папку со скриптом: cd /путь/к/скрипту
Сделать скрипт исполняемым: chmod +x script.sh
Запустить: ./script.sh (точка слеш означает «из текущей папки»).
Если скрипт лежит в одной из папок, прописанных в PATH, можно просто писать имя: script.sh.

В PowerShell:

Запуск скрипта .ps1 обычно требует указания пути: .\script.ps1.

Set-ExecutionPolicy для разрешения выполнения.
Шпаргалка по Linux (по мотивам боевых действий)
📁 Навигация и работа с файлами
Команда	Описание	Частые аргументы	Пример
ls	Показать содержимое папки	-l (подробно), -a (включая скрытые), -h (читаемые размеры)	ls -la
cd	Сменить папку		cd /home/user
pwd	Показать текущую папку		pwd
mkdir	Создать папку	-p (создать промежуточные)	mkdir -p /mnt/boot/efi
rmdir	Удалить пустую папку		
rm	Удалить файл/папку	-r (рекурсивно), -f (без подтверждения)	rm -rf старая_папка
cp	Копировать	-r (рекурсивно)	cp -r папка1 папка2
mv	Переместить/переименовать		mv file.txt new.txt
cat	Вывести содержимое файла		cat file.txt
less	Постраничный просмотр		less длинный_лог.log
head / tail	Первые/последние строки	-n 20	tail -f /var/log/syslog
nano	Простой текстовый редактор		nano config.cfg
🔍 Информация о системе и дисках
Команда	Описание	Пример
lsblk	Показать все диски и разделы	lsblk
lsblk -f	Показать с файловыми системами и UUID	lsblk -f
blkid	UUID и тип ФС разделов	sudo blkid
df -h	Свободное место на смонтированных дисках	df -h
du -sh	Размер папки/файла	du -sh /home
free -h	Оперативная память	free -h
uname -a	Информация о ядре и системе	uname -a
whoami	Текущий пользователь	whoami
id	Информация о пользователе и группах	id
systemctl status	Статус службы	systemctl status nginx
journalctl -xe	Логи системы	journalctl -xe
📦 Управление пакетами
APT (основной менеджер)
Команда	Описание
sudo apt update	Обновить список пакетов
sudo apt upgrade	Обновить все пакеты
sudo apt install пакет	Установить пакет
sudo apt remove пакет	Удалить пакет
sudo apt purge пакет	Удалить пакет с конфигами
sudo apt --fix-broken install	Починить сломанные зависимости
apt search слово	Найти пакет
WGET (скачивание файлов)
Команда	Описание
wget URL	Скачать файл по ссылке
wget -O имя URL	Скачать и сохранить под именем
wget -qO- URL	Скачать и вывести в терминал (тихо)
Примеры:

bash
wget -O discord.deb "https://discord.com/api/download?platform=linux&format=deb"
wget -qO- https://site.com/script.sh | bash  # осторожно!
Проблемы с правами при установке .deb
Ошибка: «файл недоступен для пользователя _apt»
Решение: переместить файл в /tmp

bash
sudo mv /home/user/файл.deb /tmp/
sudo apt install /tmp/файл.deb
Блокировка APT
Ошибка: «Could not get lock /var/lib/apt/lists/lock»
Решение: убить процесс-блокировщик или перезагрузить.

bash
sudo kill номер_процесса
# или просто перезагрузка
💾 Монтирование и файловые системы
Команда	Описание
mount /dev/sdX /mnt	Примонтировать раздел в папку
umount /mnt	Отмонтировать
mount --bind /dev /mnt/dev	Примонтировать системную папку в chroot
mount | grep /mnt	Проверить, что примонтировано
Создание и форматирование разделов
GParted – графическая утилита (удобно для новичков).

fdisk /dev/sdX – командная утилита (MBR).

gdisk /dev/sdX – для GPT.

mkfs.ext4 /dev/sdX1 – отформатировать в ext4.

wipefs -a /dev/sdX – стереть таблицу разделов.

Пример зачистки диска (когда всё занято):

bash
sudo umount -l /dev/sdX*
sudo wipefs -a /dev/sdX
🛠️ Работа с разделами и дисками (продвинутое)
Удаление старой таблицы разделов (жёсткий способ)
bash
sudo dd if=/dev/zero of=/dev/sdX bs=512 count=1
Временное отключение внутреннего NVMe-диска (чтобы установщик не лез)
bash
sudo modprobe -r nvme
Вернуть диск после перезагрузки – просто перезагрузить компьютер.

🔧 Восстановление загрузчика (GRUB)
Этот блок – квинтэссенция наших танцев с внешним диском.

Ситуация: после установки Mint грузится аварийная консоль GRUB
Шаги восстановления:

Загрузиться с Live-флешки Mint.

Определить разделы:

bash
lsblk
Примонтировать корневой раздел (например, /dev/sda2):

bash
sudo mount /dev/sda2 /mnt
Примонтировать ESP (если есть, обычно /dev/sda1):

bash
sudo mkdir -p /mnt/boot/efi
sudo mount /dev/sda1 /mnt/boot/efi
Примонтировать системные папки:

bash
sudo mount --bind /dev /mnt/dev
sudo mount --bind /proc /mnt/proc
sudo mount --bind /sys /mnt/sys
Зайти в систему через chroot:

bash
sudo chroot /mnt
Переустановить GRUB:

bash
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu --recheck
Для Legacy-режима: grub-install --target=i386-pc /dev/sda

Обновить конфигурацию GRUB:

bash
update-grub
Выйти и размонтировать:

bash
exit
sudo umount -R /mnt
sudo reboot
Если GRUB пишет «no working init found»
Проверить, правильно ли указан UUID корневого раздела в /etc/default/grub. Можно поправить вручную при загрузке (нажать e в меню GRUB и добавить root=UUID=...).

🔑 Права доступа и sudo
Команда	Описание
chmod 755 файл	Установить права (rwxr-xr-x)
chmod +x скрипт.sh	Сделать исполняемым
chown user:group файл	Сменить владельца
sudo -i	Войти в root (чтобы не писать sudo перед каждой командой)
sudo !!	Выполнить предыдущую команду с sudo
visudo	Редактировать файл sudoers (осторожно!)
🌐 Сеть
Команда	Описание
ip a	Показать IP-адреса
ip link	Показать сетевые интерфейсы
ping ya.ru	Проверить связь
netstat -tulpn	Показать открытые порты
ss -tulpn	Более современный аналог
curl ifconfig.me	Узнать внешний IP
wget -qO- icanhazip.com	Тоже
📝 Поиск
Команда	Описание
grep слово файл	Найти слово в файле
grep -r слово папка	Рекурсивный поиск
grep -i слово файл	Без учёта регистра
grep -n слово файл	Показать номера строк
find /путь -name "*.txt"	Найти файлы по имени
find /путь -size +100M	Найти файлы больше 100 МБ
🗜️ Архивация
Команда	Описание
tar -czf архив.tar.gz папка	Создать архив
tar -xzf архив.tar.gz	Распаковать
zip -r архив.zip папка	Создать zip-архив
unzip архив.zip	Распаковать zip
🧠 Концепции, которые мы разобрали
UEFI vs Legacy
UEFI – современный режим загрузки, требует ESP-раздел (EFI System Partition) с FAT32.

Legacy (BIOS) – старый режим, загрузчик в MBR, ESP не нужен.

GPT vs MBR
GPT – таблица разделов для UEFI, до 128 разделов, диски >2 ТБ.

MBR – для Legacy, до 4 основных разделов.

GRUB
Загрузчик, который передаёт управление ядру.

Конфигурация в /boot/grub/grub.cfg (генерируется update-grub).

chroot
Временная смена корневой директории. Позволяет зайти в «сломанную» систему и чинить её.

ESP (EFI System Partition)
Небольшой раздел (100–500 МБ), FAT32, флаги boot, esp. Содержит загрузчики (Windows Boot Manager, GRUB).

🚫 Типичные ошибки и их решения
Ошибка	Причина	Решение
Device or resource busy при wipefs	Раздел примонтирован	umount -l /dev/sdX*, затем wipefs
grub-install: cannot find EFI directory	ESP не примонтирован в /boot/efi	Примонтировать ESP: mount /dev/sdX1 /mnt/boot/efi
E: Could not get lock /var/lib/apt/lists/lock	Другой процесс apt уже запущен	Убить процесс sudo kill номер или перезагрузить
Permission denied при apt install ./пакет.deb	apt не имеет прав читать файл в домашней папке	Переместить файл в /tmp
No working init found	Ядру передан неверный UUID корня	Проверить grub.cfg, поправить при загрузке через e
📚 Полезные команды для изучения
man команда – документация (выход по q).

команда --help – краткая справка.

apropos слово – поиск команд по описанию.

which команда – показать путь к исполняемому файлу.

history – история введённых команд.




